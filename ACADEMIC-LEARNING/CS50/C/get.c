#include <stdio.h>
// dangerous code
int main(void)
{
  // WHITH STRINGS
  char s[4];
  printf("s: ");
  scanf("%s", s);
  printf("s: %s\n", s);
  // WHITH NUMBERS
  // int n;
  // printf("n: ");
  // scanf("%i", &n);
  // printf("n: %i\n", n)
}