function displayValue() {
  let stdName = ["A", "B", "C", "D", "E", "F", "G"]
  for (let i = 0; i < stdName.length; i++) {
    const element = stdName[i];
    console.log(element);
  }
}
displayValue()